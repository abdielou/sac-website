const ImageCaption = ({ children }) => (
  <div className="flex flex-wrap -mx-2 overflow-hidden xl:-mx-2">
    <div style={{ color: '#cdc332', fontSize: 'smaller' }}>{children}</div>
  </div>
)

export default ImageCaption
