---
phase: 29-id-card-system
plan: 03
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - components/member/IdCardPreview.js
  - app/member/profile/page.js
  - app/id/page.js
  - components/MemberIdCard.js
autonomous: true
requirements: [PROF-06, CLEAN-01]

must_haves:
  truths:
    - "Member can see a preview of their ID card on their profile page"
    - "The old /id page no longer exists (removed or redirects to profile)"
    - "ID card preview shows member photo, name, membership year, and QR placeholder"
  artifacts:
    - path: "components/member/IdCardPreview.js"
      provides: "Browser HTML/Tailwind ID card preview component"
      exports: ["IdCardPreview"]
    - path: "app/member/profile/page.js"
      provides: "Profile page with IdCardPreview section added"
  key_links:
    - from: "app/member/profile/page.js"
      to: "components/member/IdCardPreview.js"
      via: "import and render in profile content"
      pattern: "IdCardPreview"
    - from: "components/member/IdCardPreview.js"
      to: "/api/member/photo"
      via: "photo URL for img src"
      pattern: "api/member/photo"
---

<objective>
Add an ID card preview to the member profile page and remove the old mock /id page, completing the profile-based ID experience.

Purpose: Members should see their ID card on their own profile page instead of using the old standalone mock page. This completes the member-facing side of the ID card system.
Output: IdCardPreview component on profile page, old /id page deleted.
</objective>

<execution_context>
@C:/Users/abdie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-id-card-system/29-01-SUMMARY.md
@app/member/profile/page.js
@components/MemberIdCard.js
@app/id/page.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create browser ID card preview component and add to profile page</name>
  <files>
    components/member/IdCardPreview.js
    app/member/profile/page.js
  </files>
  <action>
    1. Create `components/member/IdCardPreview.js` — a 'use client' browser preview component:
       - Props: `{ profile }` (the member profile object from useMemberProfile hook, which includes firstName, lastName, slastName, memberSince, expirationDate, photoFileId, email)
       - Render a card with the same visual design as the PDF template but using HTML/Tailwind (NOT @react-pdf/renderer primitives):
         - Outer container: `max-w-sm mx-auto`, aspect ratio 3.375/2.125 (credit card proportions), rounded corners, shadow, overflow hidden
         - Background: #221E5A (SAC purple) with the same Saturn SVG pattern from existing MemberIdCard.js
         - SAC logo: use the SVG version (`/static/images/sac-white-logo-25.svg`) via next/image (SVG works fine in browser, unlike PDF)
         - Member photo: circular, use `/api/member/photo/${encodeURIComponent(profile.email)}?v=${profile.photoFileId || ''}` as src. If no photoFileId, show a placeholder silhouette
         - Member name: white, bold, centered — `{profile.firstName} {profile.lastName} {profile.slastName || ''}`.trim()
         - "Miembro desde {profile.memberSince}" in smaller white text
         - "Vigencia {year}" where year is extracted from profile.expirationDate (parse year from date string) — underlined
         - QR code area: show a small placeholder text "QR" in a white rounded square (the actual QR is only in the PDF; the browser preview just shows a placeholder indicating where it will appear)
       - Add a "Descargar PDF" button below the card that links to `/api/member/id-card` (we'll create this route in this task too — a member-facing version)
       - The component should handle the case where the member's status is not active — show the card dimmed with a "DRAFT" watermark overlay and disable the download button with text "Disponible cuando tu membresia este activa"

    2. Create a simple member-facing ID card download route at `app/api/member/id-card/route.js`:
       - Auth check: `req.auth` must exist, extract email from `req.auth.user.email`
       - Call `getMemberByEmail(email)` — if not found, return 404
       - If member status is not 'active' and not 'expiring-soon', return 403 "Tu membresia no esta vigente para generar tarjeta de identificacion"
       - Call `generateIdCardPdf(member)` and return PDF response (same pattern as admin route)
       - This lets members download their own ID card PDF

    3. Update `app/member/profile/page.js`:
       - Import `IdCardPreview` from `@/components/member/IdCardPreview`
       - Add an "ID de Miembro" section below the existing profile content (after the ProfileView/ProfileForm section, inside the `{profile && !isLoading && ...}` block)
       - Wrap the IdCardPreview in a section with heading "Mi Tarjeta de Identificacion" and a brief description "Vista previa de tu tarjeta de miembro SAC"
       - Add a horizontal divider (`<hr>`) between the profile section and the ID card section for visual separation
  </action>
  <verify>
    - `components/member/IdCardPreview.js` exists with 'use client' directive
    - `app/api/member/id-card/route.js` exists with GET export
    - `app/member/profile/page.js` imports and renders IdCardPreview
    - `npm run build` passes
  </verify>
  <done>Member profile page shows an ID card preview with their photo, name, and membership year; active members can download their PDF; inactive members see a dimmed preview with explanatory text</done>
</task>

<task type="auto">
  <name>Task 2: Remove old /id page and MemberIdCard component</name>
  <files>
    app/id/page.js
    components/MemberIdCard.js
  </files>
  <action>
    1. Delete `app/id/page.js` — the old standalone mock ID builder page

    2. Delete `components/MemberIdCard.js` — the old mock ID card component (no longer imported anywhere after step 1)

    3. Create a redirect from /id to /member/profile. Create `app/id/page.js` as a simple redirect:
       ```javascript
       import { redirect } from 'next/navigation'
       export default function IdRedirect() {
         redirect('/member/profile')
       }
       ```
       This ensures any existing links to /id still work by redirecting to the new location.

    4. Verify no other files import MemberIdCard or reference the old /id page:
       - Search codebase for 'MemberIdCard' imports
       - Search codebase for '/id' links (except /id-card which is different)
       - If any references found, update them to point to /member/profile
  </action>
  <verify>
    - `components/MemberIdCard.js` does not exist
    - `app/id/page.js` contains a redirect to /member/profile
    - No files import MemberIdCard (grep returns empty)
    - `npm run build` passes
  </verify>
  <done>Old mock /id page removed, replaced with redirect to /member/profile; old MemberIdCard component deleted; no dangling references</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Profile page shows ID card preview section with member data
3. Old /id page redirects to /member/profile
4. No references to deleted MemberIdCard component remain
5. Member-facing PDF download route requires authentication and active membership
</verification>

<success_criteria>
- Member can see their ID card preview on /member/profile with photo, name, year
- Active members can download their PDF via the button
- Inactive members see dimmed preview with explanation
- /id redirects to /member/profile
- No dead code from old mock ID system remains
</success_criteria>

<output>
After completion, create `.planning/phases/29-id-card-system/29-03-SUMMARY.md`
</output>
