# Use JSON array format; will be parsed with json.loads() in Python
SCOPES=["https://www.googleapis.com/auth/youtube.upload"]

# For storing autentication from OAuth 
TOKEN_FILE=token.json

# Directory for all facebook data files
FACEBOOK_DATA_DIR=facebook-sociedadastronomia

# (public, private, unlisted)
YOUTUBE_PRIVACY_STATUS=public

# Upload chunk size in bytes (-1 = upload entire file at once)
UPLOAD_CHUNK_SIZE=-1

# Facebook data directory structure (comma-separated path parts)
VIDEOS_SUBPATH=your_facebook_activity,live_videos

# Name of the registry file to track uploaded videos
REGISTRY_FILENAME=uploaded_registry.json

# 1 = enabled
OAUTH_INSECURE_TRANSPORT=1

DEFAULT_TITLE=En VIVO - Sociedad de Astronomía del Caribe
MAX_VIDEOS_PER_RUN=6
VIDEO_FILE_EXTENSIONS=.mp4
JSON_FILE=this_profile's_activity_across_facebook/live_videos/live_videos.json
YOUTUBE_API_VERSION=v3
YOUTUBE_CATEGORY_ID=22
CLIENT_SECRETS_FILE=client.json


